{
  "name": "week8-backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 5000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}